{
  "name": "pwa-theme-mosaic-sass",
  "version": "1.0.0",
  "description": "Concatenate all .scss files into a single one",
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-rename": "^1.3.0",
    "gulp-replace": "^1.0.0",
    "pump": "^3.0.0"
  }
}
